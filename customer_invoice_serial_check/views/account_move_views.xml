<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_out_invoice_tree_missing_serial_button" model="ir.ui.view">
            <field name="name">account.out.invoice.list.missing.serial.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_out_invoice_tree"/>
            <field name="arch" type="xml">
                <button name="action_send_and_print" position="before">
                    <button name="action_check_missing_customer_invoice_serials"
                            type="object"
                            string="Check Missing Serials"
                            class="btn btn-secondary"
                            groups="account.group_account_user"/>
                </button>
            </field>
        </record>

        <record id="action_check_missing_customer_invoice_serials" model="ir.actions.server">
            <field name="name">Check Missing Customer Invoice Serials</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="binding_model_id" ref="account.model_account_move"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="group_ids" eval="[(6, 0, [ref('account.group_account_user')])]"/>
            <field name="code">
action = env['account.move'].action_check_missing_customer_invoice_serials()
            </field>
        </record>
    </data>
</odoo>
