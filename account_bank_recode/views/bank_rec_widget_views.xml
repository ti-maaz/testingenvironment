<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_bank_statement_line_search_bank_rec_widget_recode" model="ir.ui.view">
            <field name="name">account.bank.statement.line.search.bank.rec.widget.recode</field>
            <field name="model">account.bank.statement.line</field>
            <field name="inherit_id" ref="account_accountant.view_bank_statement_line_search_bank_rec_widget"/>
            <field name="arch" type="xml">
                <xpath expr="//search/field[@name='move_id']" position="after">
                    <field name="reconciled_account_ids" string="Counterpart Account"/>
                </xpath>
                <xpath expr="//search/filter[@name='date']" position="after">
                    <filter
                        name="date_this_month"
                        string="This Month"
                        domain="[('date', '&gt;=', context_today().replace(day=1).strftime('%Y-%m-%d')), ('date', '&lt;=', context_today().strftime('%Y-%m-%d'))]"
                    />
                    <filter
                        name="date_last_month"
                        string="Last Month"
                        domain="[('date', '&gt;=', (context_today() + relativedelta(months=-1, day=1)).strftime('%Y-%m-%d')), ('date', '&lt;=', (context_today() + relativedelta(day=1, days=-1)).strftime('%Y-%m-%d'))]"
                    />
                </xpath>
            </field>
        </record>

        <record id="action_bank_statement_line_recode" model="ir.actions.server">
            <field name="name">Recode Transactions</field>
            <field name="model_id" ref="account.model_account_bank_statement_line"/>
            <field name="binding_model_id" ref="account.model_account_bank_statement_line"/>
            <field name="binding_view_types">list</field>
            <field name="state">code</field>
            <field name="group_ids" eval="[Command.link(ref('account.group_account_user'))]"/>
            <field name="code">
if records:
    action = records.action_open_recode_wizard()
            </field>
        </record>
    </data>
</odoo>
